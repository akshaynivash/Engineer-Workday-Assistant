# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node / frontend build output
node_modules/
frontend/dist/

# Local env files (commit .env.example instead)
.env
!.env.example

# Runtime-generated data (rebuilt automatically, not source)
data/chroma_db/
data/faiss_index/

# Large model weights (only tokenizer files like merges.txt are small enough to ship)
task-3/models/*/*.bin
task-3/models/*/*.safetensors
task-3/models/*/pytorch_model*
task-3/models/*/model.safetensors*

# OS / editor cruft
.DS_Store
Thumbs.db
